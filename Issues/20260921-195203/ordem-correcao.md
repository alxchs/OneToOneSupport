Contexto: nova rodada de homologação do dono. Relato: no Host (laptop, mouse) e no Guest (celular, dedo), lápis e pincel funcionam, mas retângulo (e formas) não; o desenho some ao soltar o botão do mouse / tirar o dedo; e nada aparece em tempo real no outro dispositivo. O chefe NÃO conseguiu reproduzir em modo dev (npm run dev), banco novo, DPR 1.0 e 1.5, convidado conectado, eventos sintéticos do CDP E entrada real do Windows (SendInput: Issues/20260921-004718/evidencia/drag-sendinput.ps1 e repro-dev-entrada-real.cjs): lápis, retângulo, elipse, linha e seta persistem no Host e chegam ao Guest. Logo, ou o ambiente do dono roda CÓDIGO DESATUALIZADO/MISTURADO (main process de uma versão, renderer ou dist/guest de outra), ou há uma condição não simulada. O objetivo desta ordem é (a) eliminar a dúvida sobre qual código roda e (b) fazer a próxima falha se auto-documentar. Não faça push nem merge. Não altere tests/adversarial/redteam-fase07.test.ts nem docs/reviews/redteam-07.md nem docs/reviews/fase-0[567].md nem docs/reviews/homologacao-1.md.

## V1 — Carimbo de versão visível
Mostre o hash curto do commit + nome da branch + data do build no rodapé do Host e no cabeçalho/rodapé discreto do Guest, e grave a mesma linha no console do main process ao iniciar e no console do renderer/Guest ao carregar. Gere o carimbo em tempo de build (vite define / script), nunca por execução de git em produção do usuário. Teste: o carimbo do Guest servido pelo servidor é IGUAL ao do Host; se dist/guest foi compilado de outro commit que o Host, o Host deve mostrar aviso claro "Guest desatualizado: rode npm run build".

## V2 — Diagnóstico sob flag
Com `ONETOONE_DIAG=1` (só em desenvolvimento; ignorar em NODE_ENV=production) registre no console, com timestamp e sem segredo/token/chave/plaintext de mensagem: path:created, finishShapeCreation (tipo, dist, descartado?), emitEvent, aplicarEventoQuadro (antes/depois: nº de elementos visíveis), retorno do IPC gravar (sucesso/erro/motivo), renderState (nº de elementos, ids removidos/adicionados), envio/recebimento no Guest (tipo, aba, resultado do reducer), descartes do servidor (motivo). Objetivo: qualquer "desenho some" deve deixar rastro dizendo POR QUE (evento não emitido, rejeitado, removido por UNDO/CLEAR, estado sobrescrito).

## V3 — Sonda com entrada real e pixels
Incorpore à tools/probe-runtime.cjs uma etapa com entrada REAL do Windows (SendInput, como drag-sendinput.ps1; chame SetProcessDPIAware antes) e arrasto nas 4 direções para lápis, pincel, retângulo, elipse, linha, seta e texto, medindo PIXELS (getImageData) no Host e no Guest, com a página do Guest em primeiro plano (bringToFront) ao medir. Rode em modo produção E em modo dev (Vite), e em DPR 1.0 e 1.5 (--force-device-scale-factor). Falha se qualquer forma não deixar pixels ou não chegar ao outro lado.

## V4 — Mapeamento de coordenadas Host x Guest
Verifique e reporte em docs/reviews/diagnostico-coordenadas.md: o canvas do Host tem largura virtual >= 900 e o do celular ~412 px. Um retângulo desenhado no Host em x>412 aparece no celular? O Guest escala o quadro inteiro para caber na tela, ou mostra só um recorte? Prove com pixels. Se houver recorte ou desalinhamento, corrija com escala uniforme (mesma regra de pointerToScene, Mestre §12) e teste nos dois sentidos.

## V5 — Script de homologação limpa
Crie tools/homologar.ps1: (1) recusa se a árvore estiver suja ou a branch não for a esperada (parâmetro); (2) encerra electron/vite antigos deste projeto; (3) apaga o banco do app (onetoone.db, -wal, -shm, snapshots em %APPDATA%\OneToOneSupport) — ordem permanente do dono, mas SÓ com o app fechado e imprimindo o que apagou; (4) roda npm run build; (5) sobe npm run dev com ONETOONE_DIAG=1; (6) imprime o carimbo de versão e o link do Guest. Teste-o de verdade (execução real) e cole a saída.

## Ao final
npm run verify verde; docs/HANDOFF.md seção "Homologação 2"; docs/reviews/autoauditoria-corr-homolog2.md (critério -> comando -> saída real -> PASS/FAIL e o que NÃO foi verificado; declare que continua sem teste em aparelho físico). Não invente nomes; não declare "nenhuma divergência" se houve.
