Contexto: LEIA AGENTS.md inteiro antes de começar, em especial a seção "Lições específicas do projeto", especialmente a lição da Fase 07 sobre visibilidade de diagnóstico. Esta é a 4ª rodada sobre o MESMO sintoma ("o desenho some ao soltar o botão/dedo; não sincroniza"). O dono já testou 3 vezes e não notou melhora, mesmo com correções reais aplicadas (dc694d8: espaço canônico 1200x800 e ordem changedTouches/touches/clientX). O chefe tentou reproduzir de forma extensiva e NÃO conseguiu: automação com CDP e com entrada real do Windows (SendInput), mouse e toque, 4 direções de arrasto, DPR 1.0 e 1.5, com e sem convidado conectado — lápis, retângulo, elipse, linha, seta e texto sempre persistiram e sincronizaram. PARE de tentar adivinhar mais uma causa. O objetivo desta ordem NÃO é corrigir um bug agora: é fazer a PRÓXIMA falha real do dono se autodocumentar sem exigir que ele abra o DevTools (ele não abre). Só depois disso, quando tivermos o log real dele, corrigiremos o bug de verdade. Não faça push nem merge. Não altere tests/adversarial/redteam-fase07.test.ts, docs/reviews/redteam-07.md, docs/reviews/fase-0[567].md, docs/reviews/homologacao-1.md.

## D1 (único objetivo obrigatório) — Encaminhar diagnóstico do renderer para o terminal
Hoje `diagLog` (src/shared/diag.ts) grava só no console do DevTools do renderer (Host e Guest), que o dono nunca abre. O processo principal já imprime no MESMO terminal onde `npm run dev` / `tools\homologar.ps1` rodam (visível sem nenhum passo extra do dono). Implemente:
1. Canal IPC novo (ex.: `diag:forward`) exposto no preload SOMENTE quando `isDiagEnabled()` for verdadeiro (reaproveite a checagem existente; nunca em produção).
2. Toda chamada a `diagLog` no Host (renderer) também envia o mesmo checkpoint+dados (já sanitizados, sem segredo) por esse canal; o processo principal recebe e faz `console.log('[DIAG-HOST] ...')` no MESMO stdout do terminal.
3. No servidor do Guest (electron/server side, que já roda no processo principal — confira `electron/server/ws.ts` e `electron/server/index.ts`), garanta que os checkpoints de recepção/decisão de eventos do Guest (autoridade, descarte, path traversal, broadcast) TAMBÉM aparecem nesse mesmo terminal, com prefixo `[DIAG-SERVER]`, sob a mesma flag.
4. NÃO é preciso instrumentar o Guest mobile em si (o celular não tem terminal acessível); o objetivo é o terminal do Host mostrar TUDO que aconteceu dos dois lados (Host e o que chegou/saiu do Guest) numa timeline só, com timestamp, para o dono copiar e colar de volta.
5. Teste automatizado: com ONETOONE_DIAG=1, desenhe (via SendInput real, reaproveitando Issues/20260921-004718/evidencia/drag-sendinput.ps1) um retângulo no Host com Guest conectado; capture o stdout do processo `app` (já é feito assim em Issues/20260921-004718/evidencia/repro-dev-entrada-real.cjs, veja `app.stdout.on('data', ...)`) e afirme que aparecem, na ordem certa, os checkpoints de: path:created/finishShapeCreation -> emitEvent -> aplicarEventoQuadro -> gravar (IPC) -> broadcastToGuest -> chegada no Guest (se houver forma de saber pelo lado do servidor) -> renderState. Cole a saída REAL do teste na autoauditoria (não invente linhas).
6. Atualize docs/HANDOFF.md com um parágrafo curto e literal para o dono: "para relatar um problema no quadro branco, feche tudo, rode tools\homologar.ps1, desenhe, e copie TODO o texto do terminal para o chefe — não precisa abrir nada além do que já abre."

## D2 — Não regredir o que já funciona
Rode `npm run verify` inteiro e a sonda com entrada real (V3, já existente) antes e depois de D1; nenhuma checagem pode piorar. Se D1 tocar em engine.ts/useHostStore.ts/GuestRoom.tsx, rode também o teste de borracha de trecho e o de sincronização bidirecional por IDs.

## Proibido
Não proponha mais uma "causa raiz" para o sumiço do desenho nesta rodada — sem o log real do terminal do dono, qualquer correção agora é adivinhação, e já tivemos 3 adivinhações. Se, ao testar D1 você mesmo perceber uma forma sumindo com o diagnóstico ativo, PARE, cole o log real do SEU terminal (não invente) em docs/reviews/diagnostico-sync-2.md, e SÓ ENTÃO proponha a correção baseada nesse log real.

## Ao final
`npm run verify` verde; docs/HANDOFF.md com a instrução literal para o dono (item D1.6); docs/reviews/autoauditoria-corr-diag-terminal.md (critério -> comando -> saída real colada -> PASS/FAIL, e o que NÃO foi verificado). Não invente nomes de arquivos/funções/linhas de log que não existem no código.
