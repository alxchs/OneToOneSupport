Contexto: HOMOLOGAÇÃO REAL do dono (Host = notebook Windows, Guest = Motorola Edge 70 Pro, Chrome/Android, mesma rede Wi-Fi) reprovou 3 itens que a sonda automática dava como PASS. A sonda tem FALSO PASS: ela conecta o Guest por loopback e conta elementos; o mundo real falhou. Trate isso como defeito do processo de verificação, não só do código. Não faça push nem merge. Não altere tests/adversarial/redteam-fase07.test.ts nem docs/reviews/redteam-07.md nem docs/reviews/fase-0[567].md.

REGRA DE ORDEM: primeiro DIAGNOSTICAR (H1), escrever a causa raiz em docs/reviews/diagnostico-sync.md (sintoma -> hipóteses testadas -> evidência de comando -> causa), e só então corrigir. Não empilhe contornos: se achar mais de um defeito, diga qual é a causa comum.

## H1 (bloqueante) — Desenhos não sincronizam entre Host e Guest, nos dois sentidos
Relato do dono: a conexão abre normalmente pelo QR (Host mostra "Convidado Conectado (E2EE)"), mas o que se desenha num lado NÃO aparece no outro, em nenhum sentido. Pistas já vistas no código (verifique, não presuma):
- `electron/ipc/evento.ipc.ts` (bloco "transmite cifrado para o Guest"): o `broadcastToGuest` está dentro de `try {} catch { /* silencioso */ }` e o retorno booleano é ignorado. Falha de envio some sem log. Proibido engolir: registre (sem segredo) e devolva o motivo.
- `src/host/HostApp.tsx` aplica eventos do Guest em UM `tabState` global e só trata DRAW_ADD/DRAW_HIDE/CLEAR_TAB e GUEST_MUTED: UNDO/REDO do Guest não são aplicados no Host nem persistidos (`electron/server/index.ts` `handleGuestEvent` só grava os 3 tipos). Confirme e corrija.
- Chave de aba: Host usa `activeAbaId` ('default'); Guest usa `msg.abaId || 'default'`; confira se os dois lados usam a MESMA chave e o mesmo formato de payload (payload de elemento com `id`), e se `renderState` do Guest/Host recebe o estado da aba ativa.
- Reproduza com a rede de verdade: suba o servidor e conecte o Guest pelo IP LAN (não 127.0.0.1) com um cliente WebSocket real (há tools/test-guest-client.ts e tests/guest-mobile.test.ts como base) E com Chromium emulando o Motorola via puppeteer apontado para o IP LAN. Compare o CONTEÚDO (ids dos elementos), não só a contagem, nos dois lados após desenhar no Host e após desenhar no Guest, e após UNDO/REDO/borracha de cada lado.
- Ative log de diagnóstico temporário (sem token/chave/plaintext de mensagem) em: decifragem no ws.ts, decisão de `canGuestExecute`, motivo de descarte em `handleGuestEvent`, retorno de `sendEncryptedToGuest`, chegada no listener do Host, `onmessage` do Guest (erros de parse/reducer). Remova ou deixe sob flag ao final.

## H2 (bloqueante) — No celular o traço aparece e depois SOME sem enviar
Relato: ao desenhar no celular o traço surge na tela e desaparece, e não chega ao Host. Investigue no Guest (src/guest/GuestRoom.tsx, src/shared/canvas/engine.ts): o traço é criado no Fabric, aplicado ao reducer local e enviado; depois `renderState` re-renderiza a partir do estado. Se o elemento do reducer não bate com o objeto do Fabric (id, tipo, coordenadas, aba), o `renderState` o remove. Verifique também se ocorre erro em `sendEncrypted` (fechamento do socket, nonce, mensagem grande demais) e se há reconexão que zera `tabStates`. Teste com toque real emulado (touchstart/move/end) com DPR 2.625 e com o pinch/scroll do navegador.

## H3 — Borracha apaga o OBJETO INTEIRO; o dono quer apagar só o trecho por onde passa
Hoje a borracha emite DRAW_HIDE do elemento inteiro (Mestre linhas 38 e 75: "borracha não deleta, esconde"). Novo requisito do dono: apagar apenas a região tocada (ex.: a ponta de um traço). Decisão do chefe (registre em ADR-012 e no HANDOFF; o dono pode vetar): 
- A "Borracha" padrão passa a ser BORRACHA DE TRECHO, compatível com event sourcing append-only: cada passada da borracha é um evento `DRAW_ADD` de um elemento do tipo máscara/apagamento (traço com largura configurável que remove os pixels dos elementos anteriores da mesma aba no render, ex.: composição destination-out numa camada só dos desenhos, com o fundo branco desenhado separado para não ser apagado). NENHUM evento é deletado ou alterado; UNDO/REDO por autor desfaz a passada da borracha como qualquer traço; snapshots e replay dão o mesmo resultado; Guest e Host renderizam igual.
- Mantenha a antiga como ferramenta separada "Borracha de objeto" (DRAW_HIDE), para não perder a função.
- Espessura da borracha configurável (reaproveite os 2/4/8/16px). Exportar PNG continua com fundo branco e sem "buracos" transparentes.
Testes: (a) apagar só a ponta de um traço deixa o restante visível (amostragem de pixels antes/depois no canvas); (b) undo da borracha restaura o trecho; (c) replay do log do zero produz o mesmo estado que o estado ao vivo; (d) borracha do Guest aparece no Host e vice-versa (depende de H1); (e) reducer continua puro e O(N) (benchmark de 50 000 eventos não pode estourar 1500 ms).

## H4 — Verificação: a sonda não pode mais dar falso PASS nem sujar o banco do dono
- A sonda (tools/probe-runtime.cjs) e os testes gravavam no banco REAL (%APPDATA%\OneToOneSupport\onetoone.db: 50 atendidos "Probe"). Faça a sonda e todo teste usarem um banco/`userData` TEMPORÁRIO isolado, apagado ao final, e nunca tocarem o banco real. Prove com comando: hash/tamanho do banco real antes e depois de `npm run verify`.
- Estenda a sonda: conectar o Guest por IP LAN e verificar sincronização BIDIRECIONAL por conteúdo (ids), incluindo UNDO/REDO e borracha de trecho.
- Acrescente ao HANDOFF uma seção "Lacuna de verificação" explicando por que o teste antigo passou e o mundo real falhou.
- O Alexandre ordenou: na fase de testes o banco é limpo a cada novo teste (até segunda ordem). Portanto o app, ao iniciar em desenvolvimento com a variável `ONETOONE_DB_RESET=1`, deve recriar o banco vazio (só em dev, nunca por padrão; recuse se NODE_ENV=production). Documente no HANDOFF.

## Ao final
`npm run verify` inteiro verde; docs/HANDOFF.md com seção "Homologação 1"; docs/reviews/autoauditoria-corr-homolog1.md (critério -> comando -> saída real -> PASS/FAIL e o que NÃO foi verificado, incluindo "não testado em aparelho físico"); ADR-012. Não invente nomes de arquivos/funções/eventos na documentação. Não declare "nenhuma divergência" se houve.
